<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/profile.css">
</head>
<body>
    <div id="profile-container">
        <form>
            <h1>Profile</h1>
            <div class="photo-container">
                <h1><%= profile.name || "Default Name" %></h1>
                <h3><%= profile.id || "No ID Provided" %></h3>
                <h4><%= profile.role || "No Role Specified" %></h4>
            </div>
            <div class="info-container">
                <h3>Username</h3>
                <h4><%= profile.username || "No Username" %></h4>
            </div>
            <div class="info-container">
                <h3>First Name</h3>
                <h4><%= profile.firstName || "No First Name" %></h4>
            </div>
            <div class="info-container">
                <h3>Last Name</h3>
                <h4><%= profile.lastName || "No Last Name" %></h4>
            </div>
            <div class="info-container">
                <h3>Email</h3>
                <h4><%= profile.email || "No Email" %></h4>
            </div>
            <div class="info-container">
                <h3>Date of Birth</h3>
                <h4><%= profile.dob || "No Date of Birth" %></h4>
            </div>
            <div class="info-container">
                <h3>Phone Number</h3>
                <h4><%= profile.phone || "No Phone Number" %></h4>
            </div>
            <div class="info-container">
                <h3>Address</h3>
                <h4><%= profile.address || "No Address" %></h4>
            </div>
            <div class="mofidy-container">  
                <button type="submit" class="modify-button">Save</button>
                <button type="button" class="edit-button">Edit</button>
            </div>
        </form>
    </div>
</body>
</html>